function numeroMax (num) {
    return Math.random() * num ;
}

console.log(numeroMax(50)); // Ele esta chamando a função numeroMax, passando o argumento 50, e como a função espera um argumento, ela retorna um número aleatório entre 0 e 50